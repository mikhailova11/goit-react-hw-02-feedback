import React from "react";
// import s from './Section.module.css'

const Section = ({title}) => {
    return (
        <section>
            <h3>{title}</h3>
        </section>

    )
}

export default Section;